{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only Tauri core window functionality required by the main UI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
